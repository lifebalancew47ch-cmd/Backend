namespace LifeBalance.OrganizationSaaS.Domain.Interfaces;

// Marker interface for entities stored in the global catalog (e.g. SaaSPlan),
// which must not be filtered by tenant.
public interface IGlobalTenantEntity
{
}
